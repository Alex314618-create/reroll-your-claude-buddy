[package]
name = "claude_buddy_portable"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1.0"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
opt-level = "z"
